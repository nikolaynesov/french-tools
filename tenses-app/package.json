{
  "name": "temps-francais-a2",
  "version": "1.0.0",
  "private": true,
  "description": "Les temps du français A2 — référence visuelle (DELF B2 project)",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/app.js && node build-standalone.mjs",
    "check": "tsc --noEmit",
    "watch": "esbuild src/main.ts --bundle --outfile=dist/app.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "playwright": "^1.62.1"
  }
}
